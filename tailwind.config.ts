// tailwind.config.ts
//
// Tailwind CSS v4 NOTE:
// Theme customization has moved from this file to src/style.css using @theme inline.
// This file is used only for plugin-level configuration (content sources,
// prefix, important, etc.). See src/style.css for all token bridges.
//
// In v4, most projects do not need a tailwind.config.ts at all — the Vite
// plugin auto-detects content. This file is kept for documentation and future use.

export default {
  // Content detection is automatic in v4 via the Vite plugin.
  // Uncomment and add paths only if you have sources outside the project root
  // that are NOT in .gitignore:
  //
  // content: [
  //   './index.html',
  //   './src/**/*.{vue,ts,tsx}',
  //   './components/**/*.vue',
  // ],
};
